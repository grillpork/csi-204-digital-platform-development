"use client";import{useEffect,useState}from"react";import{Banknote,ShoppingBag,PackageCheck,Clock}from"lucide-react";
export default function Overview(){const[orders,setOrders]=useState([]);useEffect(()=>{fetch("/api/admin/orders").then(r=>r.json()).then(j=>setOrders(j.data||[]))},[]);const revenue=orders.filter(o=>!["CANCELLED"].includes(o.status)).reduce((s,o)=>s+o.total_amount,0);const cards=[["ยอดขายรวม",`฿${revenue.toLocaleString("th-TH")}`,Banknote],["คำสั่งซื้อ",`${orders.length} รายการ`,ShoppingBag],["กำลังดำเนินการ",`${orders.filter(o=>["PAID","PROCESSING"].includes(o.status)).length} รายการ`,Clock],["จัดส่งสำเร็จ",`${orders.filter(o=>o.status==="COMPLETED").length} รายการ`,PackageCheck]];return <div><h1 className="text-2xl font-bold">ภาพรวมร้านค้า</h1><p className="mb-6 mt-1 text-sm text-slate-500">สรุปจากคำสั่งซื้อจริงในระบบ</p><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon])=><div key={label} className="rounded-2xl bg-white p-5 shadow-sm"><Icon className="mb-4 text-slate-500"/><p className="text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-bold">{value}</p></div>)}</div></div>}
